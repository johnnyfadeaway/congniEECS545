import json
import torch

if __name__ == "__main__":
    

    """
    label_dict = json.load(open("../data/id_label_mapping/LPD_id_map_genre.json", "r"))

    label_key_dict = dict()
    for key in label_dict.keys():
        if label_dict[key] in label_key_dict.keys():
            label_key_dict[label_dict[key]].append(key)
        else:
            label_key_dict[label_dict[key]] = [key]
    
    for label in label_key_dict.keys():
        print(label, len(label_key_dict[label]))
    """
    
    